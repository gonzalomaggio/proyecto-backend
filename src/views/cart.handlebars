<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <link rel="stylesheet" href="/css/cart.css">
</head>
<body>
    <h1>Shopping Cart</h1>

    <!-- Mostrar los elementos del carrito -->
    <h2>Items in Cart</h2>
    {{#if cart.items.length}}
        <ul>
            {{#each cart.items}}
                <li>
                    <h3>{{this.productId.title}}</h3> <!-- Corregido: Acceder al título del producto correctamente -->
                    <p>Description: {{this.productId.description}}</p>
                    <p>Price: ${{this.productId.price}}</p>
                    <p>Quantity: {{this.quantity}}</p>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No items in cart</p>
    {{/if}}

    <!-- Mostrar el resumen del carrito -->
    <h2>Cart Summary</h2>
    <p>Total Items: {{cart.totalItems}}</p>
    <p>Total Price: ${{cart.totalPrice}}</p>

    <button id="view-cart-btn" onclick="location.href='/api/carts/{{cart._id}}'">View Cart</button>
</body>
</html>
